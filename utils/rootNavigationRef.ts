import { createNavigationContainerRef } from '@react-navigation/native';

/** Root stack ref for drawer and other global navigation. */
export const rootNavigationRef = createNavigationContainerRef();
